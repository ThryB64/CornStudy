# External Tests Workspace

Each external experiment must live in its own `EXT###_short_name/` folder.

Required contents:

- `README.md`
- a separate feature-engineering script if needed;
- a separate evaluation script if needed;
- separated results;
- a conclusion: `KEEP`, `IMPROVE`, `REJECT` or `DATA_BLOCKED`.

Do not modify the main model from this workspace.
